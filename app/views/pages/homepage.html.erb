<div class="banner"">
  <%= image_tag "shape-home.svg", class: "home-svg" %>
  <div class="container wrapper-homepage">
    <h1 class="homepage-tagline">Bridge.</h1>
    <h4 class="tagline title gap-about">A peer-to-peer loans platform connecting bootcamp applicants to a lending community.</h4>
    <br>
    <% if user_signed_in? && current_user.applicant? %>
      <%= link_to 'My dashboard', dashboard_path, class: "btn btn-primary btn-homepage" %>
    <% elsif user_signed_in? && current_user.lender? %>
      <button type="button" class="btn btn-primary btn-homepage" data-toggle="modal" data-target="#homepageModal">Apply for a loan</button>
    <% else %>
      <%= link_to "Apply", applicant_path, class: "btn btn-primary btn-homepage" %>
    <% end %>

    <% if user_signed_in? && current_user.lender? %>
      <%= link_to 'My dashboard', dashboard_path, class: "btn btn-primary btn-homepage" %>
    <% elsif user_signed_in? && current_user.applicant? %>
      <button type="button" class="btn btn-primary btn-homepage" data-toggle="modal" data-target="#homepageModal">Invest</button>
    <% else %>
      <%= link_to "Invest", lender_path, class: "btn btn-primary btn-homepage" %>
    <% end %>
    <% if user_signed_in? %>
    <% end %>
  </div>
</div>

<div class="container wrapper-homepage">
  <h1 class="homepage-tagline gap-title">How it works.</h1>
  <p class="numbers">1</p>
  <h4 class="title gap-about">Sign up</h4>
  <p class="about">Log on to Bridge with the details of your coding bootcamp and apply for funding from a course alumni or tech professional.</p>
  <p class="numbers">2</p>
  <h1 class="title gap-about">Peer to peer</h1>
  <p class="about">Applicants are connected directly with investors through our secure platform with investors providing loans for the fundraising events of the applicants they want to fund.</p>
  <p class="numbers">3</p>
  <h1 class="title gap-about">No hidden fees or rate rises</h1>
  <p class="about">Flexible, fixed monthly payments for applicants at lower rates of interest. Alternatively you can pay the loan back in full at any point.</p>
  <p class="numbers">4</p>
  <h1 class="title gap-about">Social lending</h1>
  <p class="about">Building community by lending directly to peers and future tech professionals whilst diversifying risk across multiple loans.</p>
  <div class="row homepage-row">
    <div class="col-xs-12 col-sm-6">
      <p class="secure">100% secure lending</p>
      <div>
        <div class="banks">
          <p class="support">we support:</p>
          <div class="">
            <%= image_tag("visa.png", class: "credit-cards") %>
            <%= image_tag("american-express.png", class: "credit-cards") %>
            <%= image_tag("mastercard.png", class: "credit-cards") %>
            <%= image_tag("capital-one.png", class: "credit-cards") %>
          </div>
        </div>
      </div>

    </div>
    <div class="col-xs-12 col-sm-6">
      <p class="bootcamps">Associated Bootcamps:</p>
      <div class="bootcamp-logos">
        <%= link_to image_tag("lewagon.png", class: "lewagon-logo"), "https://www.lewagon.com/london" %>
        <p id="thirty">+ 30 other bootcamps</p>
      </div>
    </div>
  </div>

</div>
<div style="background-image: url(<%= image_path'homepage_footer.png' %>); height: 270px; margin-top: -270px;"></div>
<%= render 'shared/homepage_modal', user: current_user %>




