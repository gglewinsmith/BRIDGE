<h1>Make a loan <%= @loan.fundraising_event_sku %></h1>
<%= form_tag loan_payments_path(@loan) do %>
  <article>
    <label class="amount">
      <span>Amount: <%= humanized_money_with_symbol(@loan.amount) %></span>
    </label>
  </article>

<!--   # Commented for the lecture, remove comments.
  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-name="My Teddy"
    data-email="<%= current_user.email %>"
    data-description="Teddy <%= @order.teddy_sku %>"
    data-amount="<%= @order.amount_cents %>"
    data-currency="<%= @order.amount.currency %>"></script>

<% end %>
 -->
